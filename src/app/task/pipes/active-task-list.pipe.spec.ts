import { ActiveTaskListPipe } from './active-task-list.pipe';

describe('ActiveTaskListPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveTaskListPipe();
    expect(pipe).toBeTruthy();
  });
});
